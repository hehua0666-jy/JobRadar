# JobRadar Agent Guide

## 项目定位

JobRadar 是面向个人校园招聘求职的信息聚合、筛选和申请跟踪工具。目前主要服务于 2027 届秋招、提前批和校园招聘，同时保留部分仍可投递的 2026 届正式岗位；实习岗位不属于当前收录范围。

项目的核心价值不是建立庞大的职位数据库，而是降低反复搜索公司招聘信息、判断岗位是否值得投递以及跟踪申请状态的成本。当前阶段是“人工/AI 辅助招聘情报维护 + 静态岗位看板 + 自动变化检测”，不是完整的自动网络爬虫系统。

## 关注范围

### 岗位方向

优先关注具有较强技术相关性的岗位：

- 算法、AI、机器学习；
- 信号处理、音频/声学算法、通信/DSP；
- 感知算法；
- 匹配度较高的软件、数据及其他技术岗位。

### 企业覆盖

企业范围不局限于互联网公司，应覆盖：

- 互联网与科技公司；
- 汽车与智能驾驶；
- 通信、芯片、终端与硬件；
- 央国企、事业单位及科研院所；
- 外企和跨国公司。

企业类型筛选是核心产品能力，修改数据或界面时不得轻易破坏。

## 招聘发现与核验逻辑

招聘情报更新应优先发现“哪些新的公司开始了秋招、提前批或校园招聘”，而不是只反复检查已收录公司是否增加普通岗位。

发现新的招聘公司或项目后，再确认：

1. 是否来自官方或足够可靠的招聘信息；
2. 是否面向目标毕业年份；
3. 是否存在与关注方向匹配的岗位；
4. 招聘入口、截止时间和工作城市；
5. 信息证据等级、首次发现时间和最后核验时间。

未经核验的网络搜索结果不得自动写入正式岗位库。不确定的信息应进入观察/待核验状态，不能伪装成已确认、正在开放的岗位。岗位详情、开放状态和截止时间最终以企业官方招聘渠道为准。

## 职责边界

- ChatGPT/人工研究：联网搜索招聘信息、发现新开招聘公司、判断信息可靠性和岗位匹配度。
- JobRadar 数据文件：保存经过确认或明确标记为待核验的信息。
- Codex/coding agent：负责项目代码、数据结构、校验、测试、Git 操作和工程维护。
- GitHub Actions：负责适合自动执行的仓库级任务，例如数据差异生成和校验。
- GitHub Pages：负责静态网站发布。

Agent 不应把代码维护任务扩展成未经授权的招聘结论生成，也不应让自动化流程绕过人工核验边界。

## 当前结构与数据流

- `index.html`：页面结构和各功能区容器。
- `style.css`：紧凑卡片、列表模式、筛选区和响应式样式。
- `app.js`：加载数据，生成统计与岗位卡片，处理组合筛选、视图切换、投递标记、观察名单和变化雷达。
- `jobs.json`：当前正式岗位数据源。
- `company_watch.json`：重点公司及招聘入口监控列表。
- `changes.json`：最近一次岗位数据的新增、更新和下架变化。
- `data/jobs_previous.json`：变化检测所使用的上一版岗位快照。
- `scan_meta.json`：扫描元数据和字段语义说明。
- `version.json`：版本及数据量元信息。
- `tools/diff_jobs.py`：比较当前岗位库与旧快照，生成变化记录并更新快照。
- `.github/workflows/update-changes.yml`：在 `jobs.json` 更新或手动触发时运行变化检测并提交生成文件。

网页通过 `fetch()` 加载 `jobs.json`、`company_watch.json` 和 `changes.json`。已投递岗位 ID 与卡片/列表视图偏好只保存在浏览器 `localStorage`，不应误认为它们已同步到仓库或其他设备。

## UI 原则

- 信息密度优先，岗位卡片保持紧凑，一屏应能查看较多岗位。
- 减少无意义的纵向空间，保持页面简洁、清晰，不追求花哨效果。
- 保留卡片模式和列表模式。
- 不得轻易破坏企业类型、岗位方向、优先级、近期新增、证据等级、城市及投递状态等筛选能力。
- 新功能应尽量融入现有单页结构，避免不断增加独立页面和复杂交互。
- 响应式修改需同时考虑桌面端和移动端。
- 不要仅为视觉或代码形式更“漂亮”而改变已确认的产品行为。

## 数据原则

- `jobs.json` 是正式岗位数据的唯一当前来源；不要在代码中另建隐式岗位清单。
- `company_watch.json` 只用于重点公司、招聘项目或入口监控，不等同于已确认岗位。
- `changes.json` 用于展示岗位数据变化，不应被当作完整历史数据库。
- 岗位 `id` 应稳定且唯一；更新既有岗位时避免无理由更换 ID。
- `status`、`display_status`、`level`、`evidence_grade`、`first_seen`、`last_verified`、截止时间等字段必须保持明确、一致的语义。
- 数据结构变化必须同步检查 `app.js`、`tools/diff_jobs.py`、README、版本/扫描元数据和 GitHub Actions。
- 新增或更新数据时，应检查必填字段、重复 ID、重复岗位、重复 URL、URL 协议和 JSON 有效性。
- 不确定的信息优先标记为观察/待核验；证据不足时不得提升证据等级。
- 不要把静态 `application_status` 与浏览器本地投递状态混为一谈。

## 安全与公开仓库卫生

仓库中禁止写入：

- 密码、API Key、Token、Cookie、私钥、证书或其他凭据；
- 不必要的姓名、邮箱、手机号、求职材料等个人隐私；
- 本地绝对路径、用户名、设备信息或仅适用于个人机器的配置；
- 日志、缓存、编辑器备份、临时文件、测试垃圾文件或调试输出。

提交前检查敏感信息和新增文件。外部链接数据应视为不完全可信：输出到 HTML 时保持转义，并优先只允许合理的 `https:`/`http:` URL。除非任务明确要求，不要引入需要把密钥存入前端或仓库的功能。

## 修改规则

1. 修改前先阅读相关实现和数据，不要无理由大规模重构。
2. 尽量保持数据格式、URL、DOM 结构、筛选标签和本地存储键向后兼容。
3. 不要为了代码风格而改变已经确认的产品行为。
4. 修改前后检查 `git status` 和 `git diff`，只处理当前任务范围内的文件；保留用户已有改动。
5. 修改数据结构时，检查所有生产者和消费者，而不只修改单个文件。
6. 根据风险执行适当验证，包括 JSON 解析、Python/JavaScript 语法、数据完整性、重复项、页面加载和关键筛选行为。
7. 修改完成后说明修改了哪些文件、为什么修改以及如何验证。
8. 除非用户明确要求，否则不要自动 commit 或 push。
9. 不得执行 `git reset --hard`、强制 push、重写历史或其他破坏性 Git 操作，除非用户明确批准且已核实影响范围。
10. 不要修改与当前任务无关的文件，也不要覆盖来源不明的工作区改动。

## GitHub Actions 原则

- 自动化应执行确定性、可复现、适合仓库级运行的任务。
- 保持最小权限，谨慎对待任何具有 `contents: write` 权限并自动 push 的工作流。
- 修改岗位变化逻辑时，同时验证触发路径、旧快照、生成文件以及避免递归触发的条件。
- 新增工作流时考虑并发、分支保护、失败可见性、依赖固定和不可信输入。
- Actions 不得自动把未经人工核验的招聘搜索结果写入 `jobs.json`。

## 当前优先方向

后续工作优先考虑：

- 建立新开秋招公司的发现、核验和录入流程；
- 引入数据 Schema 和自动校验；
- 检查重复岗位、重复 ID 和重复 URL；
- 完善页面数据加载异常处理；
- 增加招聘链接有效性检查；
- 统一 README、页面和 `version.json` 的版本信息；
- 添加合适的 `.gitignore`；
- 提升 GitHub Actions 的可靠性、安全性和并发处理；
- 在不降低信息密度的前提下继续改善 UI。

优先解决真实的数据质量、可靠性和使用成本问题；避免过度工程化，也不要把当前项目擅自扩展为自动爬虫或复杂多页面系统。
